---
description: Adicionar, comitar e subir alterações para o GitHub automaticamente
---

Este workflow automatiza o processo de salvar suas alterações no GitHub.

// turbo
1. Executa o comando de git:
```powershell
git add . ; git commit -m "update: sincronização automática do EducaInvest" ; git push origin main
```
